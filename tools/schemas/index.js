const { generalInfoSchema } = require("./generalInfoSchema");
const { reservationSchema } = require("./reservationSchema");
const { productsInfoSchema } = require("./productsInfoSchema");

module.exports = { generalInfoSchema, reservationSchema, productsInfoSchema };
